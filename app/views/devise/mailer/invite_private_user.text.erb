<p class="email-greeting"><%= t("devise.mailer.invitation_instructions.hello", email: @resource.name) %></p>

<p class="email-instructions">
  <%= t("devise.mailer.invitation_instructions.someone_invited_you", application: organization_name(@resource.organization)) %>
</p>

<p class="email-button email-button__cta"><%= link_to t("devise.mailer.invitation_instructions.accept"), accept_invitation_url(@resource, invitation_token: @token, invite_redirect: decidim.root_path) %></p>

<% if @resource.invitation_due_at %>
  <p class="email-small"><%= t("devise.mailer.invitation_instructions.accept_until", due_date: l(@resource.invitation_due_at, format: :long)) %></p>
<% end %>

<p class="email-small email-closing"><%= t("devise.mailer.invitation_instructions.ignore").html_safe %></p>

Subject: 🌱 <%= t("devise.mailer.invitation_instructions.subject", application: organization_name(@resource.organization)) %>

<p class="email-greeting"><%= t("devise.mailer.invitation_instructions.hello", first_name: @resource.name) %></p>

<p class="email-instructions">
  We are inviting you to join the SOILTRIBES Platform as an active member of the Community of Practice (CoP).
  </br>
🌀 Your personal Circle is ready — a space to connect with fellow soil stewards, exchange knowledge, and co-create regenerative futures. 🌍 To begin, click the link below to access the platform and set your nickname and password:

</p>
<p class="email-button email-button__cta"><%= link_to t("devise.mailer.invitation_instructions.accept_invitation"), accept_invitation_url(@resource, invitation_token: @token, invite_redirect: decidim.root_path) %></p>

<p class="email-instructions">As part of your first steps, we invite you to complete the “Dig In” Survey — a short reflection to close the loop from our Inception Gatherings. Your input will help shape our shared agenda, tools, and protocols.</p>

<p class="email-button email-button__cta">[<%= link_to "Complete the Dig In Survey", "https://soiltribes.platoniq.net/assemblies/communityofpractice/f/22/" %>] (You'll find it directly inside your Circle after logging in)</p>

This invitation will expire on [Date + Time]. If you choose not to join, you can simply ignore this message and no account will be created.

<p class="email-instructions">🔧 Need help logging in or navigating the platform? Reach out to Harrison, our community support lead: 📩 harrison@platoniq.net</p>

<p class="email-instructions">Let's root change together,</br>The SOILTRIBES Team</p>